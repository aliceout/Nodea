# Root env — consumed by docker-compose for the Postgres service.
# Copy to `.env` (gitignored) and adjust if needed.
POSTGRES_USER=nodea
POSTGRES_PASSWORD=nodea_dev
POSTGRES_DB=nodea
POSTGRES_PORT=5432
